# StudentManagementDB
Student Management System is a C# Windows Forms (.NET 4.7.2) desktop app with SQL Server database. It includes secure login, student CRUD operations, CIA marks entry (CIA1+CIA2+CIA3) with auto grade calculation, and a ranked leaderboard — built for academic performance tracking.
